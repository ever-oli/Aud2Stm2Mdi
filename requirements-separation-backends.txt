# Optional runtime dependencies for non-Demucs separator backends.
#
# These packages are the inference/runtime subset needed to start wiring in
# architectures such as:
# - RoFormer / BS-RoFormer / Mel-Band RoFormer
# - SCNet
# - MDX23C
# - Quantized MDX variants (diffq)
#
# Sources used to choose this set:
# - ZFTurbo/Music-Source-Separation-Training requirements + inference/model code
# - starrytong/SCNet official requirements + inference code

# Shared config / runtime glue
ml_collections>=0.1.1
omegaconf>=2.2.3
PyYAML>=6.0
tqdm>=4.66.0
librosa>=0.10.2
pandas>=2.2.0
scipy>=1.11.0
wandb>=0.19.0
loralib>=0.1.2

# RoFormer family
einops>=0.8.1
beartype>=0.14.1
rotary_embedding_torch>=0.3.5
packaging>=24.0

# SCNet runtime
accelerate>=0.32.1
julius>=0.2.7

# Quantized MDX variants
diffq>=0.2.4
